[project]
name = "bar"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = ["common"]

[tool.uv.sources]
common = { workspace = true }

[build-system]
requires = ["uv_build"]
build-backend = "uv_build"
